import React from 'react';
import { BrowserRouter, Route, Switch, NavLink, Link } from 'react-router-dom';

const HelpPage = () => (
    <div>
        This text is from Help page 
    </div>
);

export default HelpPage;